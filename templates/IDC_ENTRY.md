<!-- IDC:BEGIN -->
This project uses Intent-Driven Coding (IDC). Before investigating or editing an actionable request, read the project-root `IDC.md` and capture the request with `idc start`. If the IDC CLI is unavailable, report that setup gap instead of claiming the request was recorded. Keep full workflow rules in `IDC.md` and its references; load only the relevant project Skill for the current task.
<!-- IDC:END -->
